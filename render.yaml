services:
  - type: web
    name: ivoiceup-backend
    env: python
    buildCommand: |
      sudo apt-get update && sudo apt-get install -y sqlite3
      ./deployment_script.sh
    startCommand: gunicorn employees.wsgi:application
